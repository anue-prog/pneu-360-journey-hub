

## Plan: Uploaded image as Hero background

Replace the current `hero-car.png` with the new night-road car image in the Hero section.

### Steps

1. **Copy the uploaded image** to `src/assets/hero-car-night.webp`
2. **Update `src/components/home/Hero.tsx`** — change the import from `hero-car.png` to the new image file

No other files need changes since `ServicePageHero` uses its own per-page images.

